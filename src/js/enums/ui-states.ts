/**
 * UI State
 * Enum for differentiating cell UI state as a result of click actions
 * @readonly
 * @enum {number}
 */
export enum UI_STATES {
  HIDDEN = 0,
  BEING_PRESSED = 1,
  FLAGGED = 2,
  UNCOVERED = 3
}
